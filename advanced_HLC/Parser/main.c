#include"def.h"

int main(){
	if( !stackFactory(200) ){
		error("unsuccessful Starting of Stack");
	}
	FILE* in = fopen("Files/in.txt","r");
	FILE* out = fopen("Files/out.txt","w+");
	char buffer[N];
	int i = 0;
	char tmp;
	int curr_position;


	return 0;
}
